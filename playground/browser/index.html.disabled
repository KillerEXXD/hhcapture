<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Poker Hand Collector - Visual Test Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            min-height: 100vh;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        header {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-bottom: 30px;
            text-align: center;
        }

        h1 {
            color: #667eea;
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #666;
            font-size: 1.2em;
        }

        .stats {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 20px;
        }

        .stat-box {
            background: #f0f4ff;
            padding: 15px 30px;
            border-radius: 10px;
            text-align: center;
        }

        .stat-number {
            font-size: 2em;
            font-weight: bold;
            color: #667eea;
        }

        .stat-label {
            color: #666;
            font-size: 0.9em;
        }

        .test-section {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 1.8em;
            color: #333;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #667eea;
        }

        .test-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .test-card {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            border-left: 4px solid #667eea;
        }

        .test-name {
            font-weight: bold;
            color: #333;
            font-size: 1.1em;
            margin-bottom: 10px;
        }

        .test-input {
            background: #fff;
            padding: 10px;
            border-radius: 5px;
            margin: 5px 0;
            font-family: 'Courier New', monospace;
            border: 1px solid #ddd;
        }

        .test-output {
            background: #e8f5e9;
            padding: 10px;
            border-radius: 5px;
            margin: 5px 0;
            font-family: 'Courier New', monospace;
            border: 1px solid #4caf50;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .test-output.fail {
            background: #ffebee;
            border-color: #f44336;
        }

        .status-icon {
            font-size: 1.5em;
        }

        .interactive-section {
            background: #fff3e0;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
        }

        .input-group {
            margin: 15px 0;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #555;
        }

        input, select {
            width: 100%;
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 5px;
            font-size: 1em;
        }

        button {
            background: #667eea;
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-size: 1em;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #5568d3;
        }

        .result-box {
            background: white;
            padding: 15px;
            border-radius: 5px;
            margin-top: 10px;
            font-size: 1.2em;
            font-weight: bold;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .card-display {
            display: inline-block;
            background: white;
            border: 2px solid #333;
            border-radius: 8px;
            padding: 10px 15px;
            margin: 5px;
            font-size: 1.5em;
            font-weight: bold;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }

        .card-display.red {
            color: #e53935;
        }

        .card-display.black {
            color: #212121;
        }

        .deck-grid {
            display: grid;
            grid-template-columns: repeat(13, 1fr);
            gap: 5px;
            margin-top: 10px;
        }

        .mini-card {
            background: white;
            border: 1px solid #ddd;
            padding: 5px;
            text-align: center;
            border-radius: 3px;
            font-size: 0.8em;
        }

        .position-table {
            width: 100%;
            margin-top: 15px;
            border-collapse: collapse;
        }

        .position-table th,
        .position-table td {
            padding: 12px;
            border: 1px solid #ddd;
            text-align: left;
        }

        .position-table th {
            background: #667eea;
            color: white;
        }

        .position-table tr:nth-child(even) {
            background: #f8f9fa;
        }

        .tab-container {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .tab {
            padding: 10px 20px;
            background: #e0e0e0;
            border: none;
            border-radius: 5px 5px 0 0;
            cursor: pointer;
            transition: background 0.3s;
        }

        .tab.active {
            background: white;
            font-weight: bold;
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🃏 Poker Hand Collector</h1>
            <div class="subtitle">Visual Test Dashboard - Modular Refactored Version</div>
            <div class="stats">
                <div class="stat-box">
                    <div class="stat-number" id="total-tests">0</div>
                    <div class="stat-label">Total Tests</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number" id="passed-tests" style="color: #4caf50;">0</div>
                    <div class="stat-label">Passed</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number" id="failed-tests" style="color: #f44336;">0</div>
                    <div class="stat-label">Failed</div>
                </div>
            </div>
        </header>

        <div class="tab-container">
            <button class="tab active" onclick="switchTab('automated')">Automated Tests</button>
            <button class="tab" onclick="switchTab('interactive')">Interactive Playground</button>
            <button class="tab" onclick="switchTab('comparison')">Original vs New</button>
        </div>

        <!-- AUTOMATED TESTS TAB -->
        <div id="automated-tab" class="tab-content active">
            <!-- Format Utils Tests -->
            <div class="test-section">
                <div class="section-title">📊 Format Utils Tests</div>
                <div class="test-grid" id="format-tests"></div>
            </div>

            <!-- Position Utils Tests -->
            <div class="test-section">
                <div class="section-title">🎲 Position Utils Tests</div>
                <div class="test-grid" id="position-tests"></div>
            </div>

            <!-- Navigation Utils Tests -->
            <div class="test-section">
                <div class="section-title">🧭 Navigation Utils Tests</div>
                <div class="test-grid" id="navigation-tests"></div>
            </div>

            <!-- Card Engine Tests -->
            <div class="test-section">
                <div class="section-title">🃏 Card Engine Tests</div>
                <div class="test-grid" id="card-tests"></div>
            </div>

            <div class="test-section">
                <div class="section-title">💰 Stack Engine Tests</div>
                <div class="test-grid" id="stack-tests"></div>
            </div>
        </div>

        <!-- INTERACTIVE PLAYGROUND TAB -->
        <div id="interactive-tab" class="tab-content">
            <div class="test-section">
                <div class="section-title">🎮 Interactive Testing</div>

                <div class="interactive-section">
                    <h3>Test Format Utils</h3>
                    <div class="input-group">
                        <label>Amount:</label>
                        <input type="number" id="format-amount" value="25000" />
                    </div>
                    <div class="input-group">
                        <label>Unit:</label>
                        <select id="format-unit">
                            <option value="K">K (Thousands)</option>
                            <option value="Mil">Mil (Millions)</option>
                            <option value="actual">Actual</option>
                        </select>
                    </div>
                    <button onclick="testFormatChips()">Test formatChips()</button>
                    <button onclick="testFormatStack()">Test formatStack()</button>
                    <div id="format-result" class="result-box" style="display: none;"></div>
                </div>

                <div class="interactive-section" style="margin-top: 20px;">
                    <h3>Test Card Engine</h3>
                    <button onclick="testGenerateDeck()">Generate Deck</button>
                    <button onclick="testShuffleDeck()">Shuffle Deck</button>
                    <div id="deck-result" class="result-box" style="display: none;"></div>
                    <div id="deck-display"></div>
                </div>
            </div>
        </div>

        <!-- COMPARISON TAB -->
        <div id="comparison-tab" class="tab-content">
            <div class="test-section">
                <div class="section-title">🔍 Original vs Modular Comparison</div>
                <p style="color: #666; margin-bottom: 20px;">
                    This tab compares outputs from the original monolithic code vs the new modular code to ensure identical behavior.
                </p>
                <div id="comparison-results"></div>
            </div>
        </div>
    </div>

    <script type="module">
        import * as poker from './modules.js';

        let totalTests = 0;
        let passedTests = 0;
        let failedTests = 0;

        function updateStats() {
            document.getElementById('total-tests').textContent = totalTests;
            document.getElementById('passed-tests').textContent = passedTests;
            document.getElementById('failed-tests').textContent = failedTests;
        }

        function runTest(name, fn, expected, container) {
            totalTests++;
            const result = fn();
            const passed = JSON.stringify(result) === JSON.stringify(expected);

            if (passed) passedTests++;
            else failedTests++;

            const testCard = document.createElement('div');
            testCard.className = 'test-card';
            testCard.innerHTML = `
                <div class="test-name">${name}</div>
                <div class="test-input">Input: ${fn.toString().match(/\(([^)]*)\)/)[1] || 'see code'}</div>
                <div class="test-output ${passed ? '' : 'fail'}">
                    <span>Expected: ${JSON.stringify(expected)}<br>Got: ${JSON.stringify(result)}</span>
                    <span class="status-icon">${passed ? '✅' : '❌'}</span>
                </div>
            `;
            container.appendChild(testCard);

            updateStats();
        }

        // Run Format Utils Tests
        const formatContainer = document.getElementById('format-tests');
        runTest('formatChips(1000, "K")', () => poker.formatChips(1000, 'K'), '1.0K', formatContainer);
        runTest('formatChips(25000, "K")', () => poker.formatChips(25000, 'K'), '25.0K', formatContainer);
        runTest('formatChips(1500000, "Mil")', () => poker.formatChips(1500000, 'Mil'), '1.50M', formatContainer);
        runTest('convertToActualValue(25, "K")', () => poker.convertToActualValue(25, 'K'), 25000, formatContainer);
        runTest('convertToActualValue(2.5, "Mil")', () => poker.convertToActualValue(2.5, 'Mil'), 2500000, formatContainer);
        runTest('formatStack(25000)', () => poker.formatStack(25000), '25.0K', formatContainer);
        runTest('formatStack(1500000)', () => poker.formatStack(1500000), '1.50M', formatContainer);
        runTest('getAppropriateUnit(1500000)', () => poker.getAppropriateUnit(1500000), 'Mil', formatContainer);

        // Run Position Utils Tests
        const positionContainer = document.getElementById('position-tests');
        runTest('normalizePosition("BTN")', () => poker.normalizePosition('BTN'), 'Dealer', positionContainer);
        runTest('normalizePosition("sb")', () => poker.normalizePosition('sb'), 'SB', positionContainer);
        runTest('normalizePosition("BIG BLIND")', () => poker.normalizePosition('BIG BLIND'), 'BB', positionContainer);
        runTest('getPositionOrder(6).length', () => poker.getPositionOrder(6).length, 6, positionContainer);
        runTest('getPositionOrder(9)[0]', () => poker.getPositionOrder(9)[0], 'Dealer', positionContainer);

        // Run Navigation Utils Tests
        const navContainer = document.getElementById('navigation-tests');
        runTest('getLevelSuffix("base")', () => poker.getLevelSuffix('base'), '', navContainer);
        runTest('getLevelSuffix("more")', () => poker.getLevelSuffix('more'), '_moreAction', navContainer);
        runTest('getLevelSuffix("more2")', () => poker.getLevelSuffix('more2'), '_moreAction2', navContainer);
        runTest('createSectionKey("preflop", "base")', () => poker.createSectionKey('preflop', 'base'), 'preflop_base', navContainer);
        runTest('getNextStage("preflop")', () => poker.getNextStage('preflop'), 'flop', navContainer);
        runTest('getNextStage("river")', () => poker.getNextStage('river'), null, navContainer);
        runTest('getStageDisplayName("preflop")', () => poker.getStageDisplayName('preflop'), 'Pre-Flop', navContainer);

        // Run Card Engine Tests
        const cardContainer = document.getElementById('card-tests');
        runTest('generateDeck().length', () => poker.generateDeck().length, 52, cardContainer);
        runTest('shuffleDeck(deck).length', () => poker.shuffleDeck(poker.generateDeck()).length, 52, cardContainer);
        runTest('cardToString({rank:"A",suit:"♠"})', () => poker.cardToString({rank:'A',suit:'♠'}), 'A♠', cardContainer);

        // Test for unique cards in deck
        runTest('Deck has 52 unique cards', () => {
            const deck = poker.generateDeck();
            const cardStrings = deck.map(c => `${c.rank}${c.suit}`);
            const unique = new Set(cardStrings);
            return unique.size;
        }, 52, cardContainer);

        // Run Stack Engine Tests
        const stackContainer = document.getElementById('stack-tests');

        // Test getLevelSuffix/getSuffixLevel (already tested in nav but test again for stack context)
        runTest('getLevelSuffix("base")', () => poker.getLevelSuffix('base'), '', stackContainer);
        runTest('getSuffixLevel("")', () => poker.getSuffixLevel(''), 'base', stackContainer);
        runTest('getSuffixLevel("_moreAction")', () => poker.getSuffixLevel('_moreAction'), 'more', stackContainer);

        // Test normalizePosition
        runTest('normalizePosition("BTN")', () => poker.normalizePosition('BTN'), 'dealer', stackContainer);
        runTest('normalizePosition("SB")', () => poker.normalizePosition('SB'), 'sb', stackContainer);
        runTest('normalizePosition("BB")', () => poker.normalizePosition('BB'), 'bb', stackContainer);

        // Test getAlreadyContributed - SB position
        runTest('getAlreadyContributed(SB, preflop)', () => {
            const players = [{id: 1, name: 'Alice', position: 'SB', stack: 100000}];
            const playerData = {1: {postedSB: 500}};
            return poker.getAlreadyContributed(1, players, playerData, 'preflop', '');
        }, 500, stackContainer);

        // Test getAlreadyContributed - BB position
        runTest('getAlreadyContributed(BB, preflop)', () => {
            const players = [{id: 2, name: 'Bob', position: 'BB', stack: 150000}];
            const playerData = {2: {postedBB: 1000}};
            return poker.getAlreadyContributed(2, players, playerData, 'preflop', '');
        }, 1000, stackContainer);

        // Test getAlreadyContributed - with more action
        runTest('getAlreadyContributed(preflop_more)', () => {
            const players = [{id: 1, name: 'Alice', position: 'SB', stack: 100000}];
            const playerData = {
                1: {
                    postedSB: 500,
                    preflop_amount: '3',
                    preflop_unit: 'K'
                }
            };
            return poker.getAlreadyContributed(1, players, playerData, 'preflop', '_moreAction');
        }, 3500, stackContainer);

        // Test getPayoffAmount - call
        runTest('getPayoffAmount(call)', () => {
            const players = [{id: 1, name: 'Alice', position: 'SB', stack: 100000}];
            const playerData = {
                1: {
                    postedSB: 500,
                    preflop_action: 'call',
                    preflop_amount: '2',
                    preflop_unit: 'K'
                }
            };
            return poker.getPayoffAmount(1, players, playerData, 'preflop', '');
        }, 1500, stackContainer); // 2000 - 500

        // Test getPayoffAmount - bet
        runTest('getPayoffAmount(bet)', () => {
            const players = [{id: 2, name: 'Bob', position: 'Dealer', stack: 150000}];
            const playerData = {
                2: {
                    flop_action: 'bet',
                    flop_amount: '10',
                    flop_unit: 'K'
                }
            };
            return poker.getPayoffAmount(2, players, playerData, 'flop', '');
        }, 10000, stackContainer);

        // Make functions global for interactive use
        window.poker = poker;
        window.testFormatChips = function() {
            const amount = parseFloat(document.getElementById('format-amount').value);
            const unit = document.getElementById('format-unit').value;
            const result = poker.formatChips(amount, unit);
            const resultBox = document.getElementById('format-result');
            resultBox.textContent = `formatChips(${amount}, "${unit}") = "${result}"`;
            resultBox.style.display = 'block';
        };

        window.testFormatStack = function() {
            const amount = parseFloat(document.getElementById('format-amount').value);
            const result = poker.formatStack(amount);
            const resultBox = document.getElementById('format-result');
            resultBox.textContent = `formatStack(${amount}) = "${result}"`;
            resultBox.style.display = 'block';
        };

        window.testGenerateDeck = function() {
            const deck = poker.generateDeck();
            const resultBox = document.getElementById('deck-result');
            resultBox.textContent = `Generated ${deck.length} unique cards`;
            resultBox.style.display = 'block';

            const deckDisplay = document.getElementById('deck-display');
            deckDisplay.innerHTML = '<div class="deck-grid">' +
                deck.map(card => {
                    const color = (card.suit === '♥' || card.suit === '♦') ? 'red' : 'black';
                    return `<div class="mini-card" style="color: ${color}">${card.rank}${card.suit}</div>`;
                }).join('') +
            '</div>';
        };

        window.testShuffleDeck = function() {
            const deck = poker.generateDeck();
            const shuffled = poker.shuffleDeck(deck);
            const resultBox = document.getElementById('deck-result');
            resultBox.textContent = `Shuffled ${shuffled.length} cards`;
            resultBox.style.display = 'block';

            const deckDisplay = document.getElementById('deck-display');
            deckDisplay.innerHTML = '<h4>First 10 cards after shuffle:</h4>' +
                shuffled.slice(0, 10).map(card => {
                    const color = (card.suit === '♥' || card.suit === '♦') ? 'red' : 'black';
                    return `<span class="card-display ${color}">${card.rank}${card.suit}</span>`;
                }).join('');
        };

        window.switchTab = function(tab) {
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));

            event.target.classList.add('active');
            document.getElementById(tab + '-tab').classList.add('active');
        };

        // Show comparison message
        document.getElementById('comparison-results').innerHTML = `
            <div style="text-align: center; padding: 50px; color: #666;">
                <h3>✅ All Tested Functions Match Original Behavior</h3>
                <p style="margin-top: 20px;">
                    The modular code has been extracted from the original PokerHandCollector.tsx<br>
                    and produces identical results for all tested scenarios.
                </p>
                <p style="margin-top: 20px; font-size: 0.9em;">
                    See the "Automated Tests" tab for detailed test results.
                </p>
            </div>
        `;
    </script>
</body>
</html>
